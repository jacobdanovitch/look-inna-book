import React from 'react';

type BookProps = {
    asin: string,
    title: string,
    author: string,
    image_url: string,
    category: string,
    category_id: string,
};

export default BookProps;